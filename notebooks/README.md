# Notebooks

This directory is reserved for optional interactive documentation
(R Markdown or Jupyter notebooks) that walks through the analysis.

The core analysis is implemented as modular R scripts in `scripts/`
(see the main README for the execution order). No notebooks are
required to reproduce the results; this folder is provided so that
a browsable workflow can be added later without restructuring the
repository.
